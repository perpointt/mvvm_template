// GENERATED CODE - DO NOT MODIFY BY HAND

part of 'paddings.dart';

// **************************************************************************
// PaddingerGenerator
// **************************************************************************

// **************************************************************************
// START Medium
// **************************************************************************

class MediumAllPadding extends Padding {
  const MediumAllPadding({Key? key, required Widget child})
      : super(
            key: key,
            padding: const EdgeInsets.all(PADDING_MEDIUM),
            child: child);
}

class MediumLeftPadding extends Padding {
  const MediumLeftPadding({Key? key, required Widget child})
      : super(
            key: key,
            padding: const EdgeInsets.only(left: PADDING_MEDIUM),
            child: child);
}

class MediumTopPadding extends Padding {
  const MediumTopPadding({Key? key, required Widget child})
      : super(
            key: key,
            padding: const EdgeInsets.only(top: PADDING_MEDIUM),
            child: child);
}

class MediumRightPadding extends Padding {
  const MediumRightPadding({Key? key, required Widget child})
      : super(
            key: key,
            padding: const EdgeInsets.only(right: PADDING_MEDIUM),
            child: child);
}

class MediumBottomPadding extends Padding {
  const MediumBottomPadding({Key? key, required Widget child})
      : super(
            key: key,
            padding: const EdgeInsets.only(bottom: PADDING_MEDIUM),
            child: child);
}

class MediumHorizontalPadding extends Padding {
  const MediumHorizontalPadding({Key? key, required Widget child})
      : super(
            key: key,
            padding: const EdgeInsets.symmetric(horizontal: PADDING_MEDIUM),
            child: child);
}

class MediumVerticalPadding extends Padding {
  const MediumVerticalPadding({Key? key, required Widget child})
      : super(
            key: key,
            padding: const EdgeInsets.symmetric(vertical: PADDING_MEDIUM),
            child: child);
}

class MediumLeftTopPadding extends Padding {
  const MediumLeftTopPadding({Key? key, required Widget child})
      : super(
            key: key,
            padding: const EdgeInsets.only(
                left: PADDING_MEDIUM, top: PADDING_MEDIUM),
            child: child);
}

class MediumLeftBottomPadding extends Padding {
  const MediumLeftBottomPadding({Key? key, required Widget child})
      : super(
            key: key,
            padding: const EdgeInsets.only(
                left: PADDING_MEDIUM, bottom: PADDING_MEDIUM),
            child: child);
}

class MediumRightTopPadding extends Padding {
  const MediumRightTopPadding({Key? key, required Widget child})
      : super(
            key: key,
            padding: const EdgeInsets.only(
                right: PADDING_MEDIUM, top: PADDING_MEDIUM),
            child: child);
}

class MediumRightBottomPadding extends Padding {
  const MediumRightBottomPadding({Key? key, required Widget child})
      : super(
            key: key,
            padding: const EdgeInsets.only(
                right: PADDING_MEDIUM, bottom: PADDING_MEDIUM),
            child: child);
}

// **************************************************************************
// END Medium
// **************************************************************************

// **************************************************************************
// START Large
// **************************************************************************

class LargeAllPadding extends Padding {
  const LargeAllPadding({Key? key, required Widget child})
      : super(
            key: key,
            padding: const EdgeInsets.all(PADDING_LARGE),
            child: child);
}

class LargeLeftPadding extends Padding {
  const LargeLeftPadding({Key? key, required Widget child})
      : super(
            key: key,
            padding: const EdgeInsets.only(left: PADDING_LARGE),
            child: child);
}

class LargeTopPadding extends Padding {
  const LargeTopPadding({Key? key, required Widget child})
      : super(
            key: key,
            padding: const EdgeInsets.only(top: PADDING_LARGE),
            child: child);
}

class LargeRightPadding extends Padding {
  const LargeRightPadding({Key? key, required Widget child})
      : super(
            key: key,
            padding: const EdgeInsets.only(right: PADDING_LARGE),
            child: child);
}

class LargeBottomPadding extends Padding {
  const LargeBottomPadding({Key? key, required Widget child})
      : super(
            key: key,
            padding: const EdgeInsets.only(bottom: PADDING_LARGE),
            child: child);
}

class LargeHorizontalPadding extends Padding {
  const LargeHorizontalPadding({Key? key, required Widget child})
      : super(
            key: key,
            padding: const EdgeInsets.symmetric(horizontal: PADDING_LARGE),
            child: child);
}

class LargeVerticalPadding extends Padding {
  const LargeVerticalPadding({Key? key, required Widget child})
      : super(
            key: key,
            padding: const EdgeInsets.symmetric(vertical: PADDING_LARGE),
            child: child);
}

class LargeLeftTopPadding extends Padding {
  const LargeLeftTopPadding({Key? key, required Widget child})
      : super(
            key: key,
            padding:
                const EdgeInsets.only(left: PADDING_LARGE, top: PADDING_LARGE),
            child: child);
}

class LargeLeftBottomPadding extends Padding {
  const LargeLeftBottomPadding({Key? key, required Widget child})
      : super(
            key: key,
            padding: const EdgeInsets.only(
                left: PADDING_LARGE, bottom: PADDING_LARGE),
            child: child);
}

class LargeRightTopPadding extends Padding {
  const LargeRightTopPadding({Key? key, required Widget child})
      : super(
            key: key,
            padding:
                const EdgeInsets.only(right: PADDING_LARGE, top: PADDING_LARGE),
            child: child);
}

class LargeRightBottomPadding extends Padding {
  const LargeRightBottomPadding({Key? key, required Widget child})
      : super(
            key: key,
            padding: const EdgeInsets.only(
                right: PADDING_LARGE, bottom: PADDING_LARGE),
            child: child);
}

// **************************************************************************
// END Large
// **************************************************************************

// **************************************************************************
// START Extreme
// **************************************************************************

class ExtremeAllPadding extends Padding {
  const ExtremeAllPadding({Key? key, required Widget child})
      : super(
            key: key,
            padding: const EdgeInsets.all(PADDING_EXTREME),
            child: child);
}

class ExtremeLeftPadding extends Padding {
  const ExtremeLeftPadding({Key? key, required Widget child})
      : super(
            key: key,
            padding: const EdgeInsets.only(left: PADDING_EXTREME),
            child: child);
}

class ExtremeTopPadding extends Padding {
  const ExtremeTopPadding({Key? key, required Widget child})
      : super(
            key: key,
            padding: const EdgeInsets.only(top: PADDING_EXTREME),
            child: child);
}

class ExtremeRightPadding extends Padding {
  const ExtremeRightPadding({Key? key, required Widget child})
      : super(
            key: key,
            padding: const EdgeInsets.only(right: PADDING_EXTREME),
            child: child);
}

class ExtremeBottomPadding extends Padding {
  const ExtremeBottomPadding({Key? key, required Widget child})
      : super(
            key: key,
            padding: const EdgeInsets.only(bottom: PADDING_EXTREME),
            child: child);
}

class ExtremeHorizontalPadding extends Padding {
  const ExtremeHorizontalPadding({Key? key, required Widget child})
      : super(
            key: key,
            padding: const EdgeInsets.symmetric(horizontal: PADDING_EXTREME),
            child: child);
}

class ExtremeVerticalPadding extends Padding {
  const ExtremeVerticalPadding({Key? key, required Widget child})
      : super(
            key: key,
            padding: const EdgeInsets.symmetric(vertical: PADDING_EXTREME),
            child: child);
}

class ExtremeLeftTopPadding extends Padding {
  const ExtremeLeftTopPadding({Key? key, required Widget child})
      : super(
            key: key,
            padding: const EdgeInsets.only(
                left: PADDING_EXTREME, top: PADDING_EXTREME),
            child: child);
}

class ExtremeLeftBottomPadding extends Padding {
  const ExtremeLeftBottomPadding({Key? key, required Widget child})
      : super(
            key: key,
            padding: const EdgeInsets.only(
                left: PADDING_EXTREME, bottom: PADDING_EXTREME),
            child: child);
}

class ExtremeRightTopPadding extends Padding {
  const ExtremeRightTopPadding({Key? key, required Widget child})
      : super(
            key: key,
            padding: const EdgeInsets.only(
                right: PADDING_EXTREME, top: PADDING_EXTREME),
            child: child);
}

class ExtremeRightBottomPadding extends Padding {
  const ExtremeRightBottomPadding({Key? key, required Widget child})
      : super(
            key: key,
            padding: const EdgeInsets.only(
                right: PADDING_EXTREME, bottom: PADDING_EXTREME),
            child: child);
}

// **************************************************************************
// END Extreme
// **************************************************************************
